pre-commit:
  jobs:
    - run: pnpm exec vp staged
      stage_fixed: true

    - run: pnpm exec vp check --fix
      stage_fixed: true

commit-msg:
  commands:
    "lint commit message":
      run: pnpm exec commitlint --edit {1}
